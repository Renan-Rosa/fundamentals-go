package names

// Função pública (inicia com letra maiúscula)
func Somar(a, b int) int {
	return a + b
}

// Função privada (inicia com letra minúscula)
func subtrair(a, b int) int { // Veja que ocorre um erro por não estar sendo usada
	return a - b
}
